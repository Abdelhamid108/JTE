fields {
    required {
    }
    optional {
        repoUrl             = String    
        gitCreds            = String    
        terraformBranchName = String    

        infra_dir           = String    
        tf_vars             = String    

        cloud_creds         = String    

        ansible_dir         = String

        inventory_file      = String    // Ansible inventory file generated by Terraform (e.g. local_file resource). Default: 'hosts.ini'

        is_destroy          = Boolean   
        install_tools       = Boolean   

        softFail            = Boolean   
    }
}

steps {
    checkoutCode
    installTools
    init
    validate
    checkov
    plan
    approval
    deploy
    destroy
    archiveInventory
    terratest
}
